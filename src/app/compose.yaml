services:
  app:
    image: feloraphp/swoole
    container_name: feloraphp
    environment:
      AUTORELOAD_PROGRAMS: "swoole"
      AUTORELOAD_ANY_FILES: 0
    ports:
      - ${PORT:-9501}:${PORT:-9501}
    volumes:
      - ..:/var/www/feloraphp